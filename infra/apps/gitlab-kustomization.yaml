apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: gitlab
  namespace: flux-system
spec:
  interval: 1m
  path: "./infra/apps/gitlab/conf"
  prune: true
  sourceRef:
    kind: GitRepository
    name: infrastructure-repo
