apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: myapp
    component: redis
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
  selector:
    app: myapp
    component: redis
