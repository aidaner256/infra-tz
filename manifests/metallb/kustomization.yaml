apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: metallb
  namespace: flux-system
spec:
  interval: 1m
  path: "./manifests/metallb/conf"
  prune: true
  sourceRef:
    kind: GitRepository
    name: metallb-repo
